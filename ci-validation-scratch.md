# CI validation scratch

Throwaway file: validates that a docs-only PR takes the fast path under
paths-filter v4.0.1 (heavy jobs skip, ci-passed still reports green).
This PR is closed unmerged — see #18.
